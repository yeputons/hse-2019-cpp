#include <algorithm>
#include <iostream>
#include <iterator>
#include <random>

using namespace std;

int main() {
    int a[] = {1, 2, 3, 4, 5};
    int b[] = {3, 1, 5, 2, 5};
    // При помощи std::inner_product выведите количество индексов `i` таких, что a[i] > b[i].
    TODO
}
